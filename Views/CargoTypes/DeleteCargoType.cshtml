@model CargoType
@{
    ViewBag.Title = "Удаление вида груза";
}

<div>
    <dl class="dl-horizontal">
        <dt>Вы хотите удалить вид груза с наименованием:</dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <div>
        <form asp-controller="CargoTypes" asp-action="Delete" method="post" asp-route-id="@Model.CargoTypeId">
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Удалить" />
            </div>
        </form>
    </div>
</div>