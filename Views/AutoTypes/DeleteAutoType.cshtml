@model AutoType
@{
    ViewBag.Title = "Удаление вида автомобиля";
}

<div>
    <dl class="dl-horizontal">
        <dt>Вы хотите удалить вид автомобиля с наименованием:</dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <div>
        <form asp-controller="AutoTypes" asp-action="Delete" method="post" asp-route-id="@Model.AutoTypeId">
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Удалить" />
            </div>
        </form>
    </div>
</div>