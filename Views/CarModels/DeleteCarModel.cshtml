@model CarModel
@{
    ViewBag.Title = "Удаление марки автомобиля";
}

<div>
    <dl class="dl-horizontal">
        <dt>Вы хотите удалить марку автомобиля с наименованием:</dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>

    <div>
        <form asp-controller="CarModels" asp-action="Delete" method="post" asp-route-id="@Model.CarModelId">
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Удалить" />
            </div>
        </form>
    </div>
</div>